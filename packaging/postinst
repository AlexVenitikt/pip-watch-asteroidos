#!/bin/sh
set -e

if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload || true
    systemctl enable --now pipboy-telemetry.service || true
    systemctl restart user@1000.service || true
fi

exit 0
